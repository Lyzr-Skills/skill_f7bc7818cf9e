# Option Validation Module

## Instructions:
1. Validate that all candidate options conform to `schemas/option.json`.
2. Check for unique `option_id`, name, and estimated dimensions.
3. Reject options that lack required descriptive information.
